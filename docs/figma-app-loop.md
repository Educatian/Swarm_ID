# DTS Figma ↔ App 운영 루프

Figma 파일 `4WIyk1eds5QF6B8fV9fpTU`를 UI/UX 설계 기준으로 사용한다.

## 적용 순서

1. `preview.html`과 `app.js`에서 역할·뷰·상태·반응형 변형을 확인한다.
2. 실행 중인 화면을 Figma에 캡처하고, 캡처 노드 이름을 `Current · <route/state>`로 고정한다.
3. Figma `01 · UX Audit & Decisions`에서 정보 우선순위, 텍스트 잘림, 간격, 접근성, 모바일 작업 맥락을 검토한다.
4. Figma `02 · Improved Full App`의 개선안을 기준으로 `styles.css`/`app.js`에 반영한다.
5. 데스크톱·모바일 브라우저를 확인하고 `node scripts/smoke-usability-upgrade.mjs`를 실행한다.
6. 변경된 화면을 다시 캡처해 Figma의 Current 캡처와 비교하고, 불일치가 있으면 3단계로 되돌아간다.

## 현재 커버리지

캡처는 두 차례에 걸쳐 만들어졌고, 2차 배치는 섹션에 넣지 않은 채로 남아 있다.
아래 표의 "위치"는 2026-07-31 Figma 실물 판독 기준이다.

| 앱 화면 | Figma Current 노드 | 위치 |
| --- | --- | --- |
| 랜딩 | `18:2` | 섹션 `17:3` 안 |
| 학습자 홈 | `19:2` | 섹션 `17:3` 안 |
| 학습자 홈 · 모바일 | `37:2` | 페이지 직속 (x=24560) |
| 네트워크 | `33:2` | 페이지 직속 (x=19280) |
| 관점 | `31:2` | 페이지 직속 (x=16640) |
| 상충 관계 | `32:2` | 페이지 직속 (x=17960) |
| 실험 공간 | `43:2` | 섹션 `17:3` 안 |
| 리포트 | `34:2` | 페이지 직속 (x=20600) |
| 교수자 개요 | `35:2` | 페이지 직속 (x=21920) |
| 교수자 관리 | `36:2` | 페이지 직속 (x=23240) |

1차 배치의 잔재 2개가 아직 살아 있고, 이름이 2차 배치와 겹친다. 어느 쪽을 볼지
헷갈리므로 캡처를 참조할 때는 노드 ID로 지목한다.

| 잔재 노드 | 이름 | 겹치는 노드 |
| --- | --- | --- |
| `21:2` (섹션 `17:3` 안) | Current · Network | `33:2`와 동명 |
| `22:2` (섹션 `17:3` 안) | Current · Instructor manage | `36:2`와 동명 |

즉 Current 캡처는 총 **12개**이며, 그중 섹션 `17:3`에 들어 있는 것은 5개
(`18:2`, `19:2`, `21:2`, `22:2`, `43:2`)뿐이다. 나머지 7개는 페이지 캔버스
x=16640~24560에 떠 있어 섹션 경계(0~6600) 밖이다.

## 파일 구조

작업 페이지는 `17:2 · 03 · Full App UI UX Loop`이고, 그 아래 섹션은 다음과 같다.

| 섹션 | 이름 | 크기 | 실제 자식 (2026-07-31 판독) |
| --- | --- | --- | --- |
| `17:3` | 00 · Current Product Captures | 6600×4200 | `18:2`, `19:2`, `21:2`, `22:2`, `43:2` — 5개뿐 |
| `17:4` | 01 · UX Audit & Decisions · 2026-07-30 | 3000×5600 | 표제 `50:2`, 증거 캡처 `49:2`–`49:5`·`51:2`·`51:3`, 발견 카드 `50:5`·`50:8`·`50:11`·`50:14`·`50:17`·`51:4`·`51:7`, 선행 보드 `26:2`·`40:2` |
| `17:5` | 02 · Improved Full App | 6600×5200 | 구현 기록 `52:2`, 교사 관심사 제안 `46:2`, 콘솔 시안 `26:41`, 잔여 화면 시안 `40:25` |
| `17:6` | 03 · DTS Design System | 9800×1800 | 운영 규칙 보드 `44:2 · Figma ↔ App Operating Loop` |

정리된 교수자 콘솔 시안은 `26:41 · Instructor · Clean operations`다.

`40:2 · Audit · Remaining views`와 `40:25 · Remaining · Clean states`는 stale이 아니다.
각각 `17:4`/`17:5`의 자식으로 살아 있다. 최상위가 아니라 섹션 아래에 있어서 페이지
직속 조회로는 보이지 않을 뿐이다.

이 파일에는 페이지가 3개 있다: `0:1 · Page 1`, `5:2 · 02 · Redesigned Experience`,
`17:2 · 03 · Full App UI UX Loop`. 앱 UI/UX 루프의 기준은 `17:2`다.

### 알려진 결함

감사 카드 5개의 헤드라인이 발견 제목 대신 우선순위를 두 번 출력한다
(`50:5`/`50:14` "P1  P1", `50:8`/`50:11` "P0  P0", `50:17` "P2  P2 · 공통 시스템 개선").
보드를 만든 스크립트가 `card(x, y, w, h, title, body, severity)`에 제목 대신
우선순위를 넘겼고, 실제 제목은 본문 첫 줄로 밀려 본문과 구분되지 않는다.
`51:4`/`51:7`은 다른 함수로 만들어져 정상이다.

교수자 콘솔 감사는 별도 파일 `uumUXm2LdFzra2VM5UDxiJ`(DTS Instructor Console — Hyeji UX Audit)에도 있다. 두 파일이 갈라지지 않도록, 화면 구조의 기준은 항상 `4WIyk1eds5QF6B8fV9fpTU`로 둔다.

그 파일은 2026-07-31 판독 기준 페이지 1개(`0:1`)에 프레임 2개뿐이다 —
`1:2 · 디자인 텐션 스튜디오`(앱 캡처)와 `3:2 · DTS / 교수자 운영 콘솔 / Hyeji`(시안),
둘 다 1440×1000. 단일 화면 탐색용이라 SSoT가 갈라질 위험은 낮다.

## 검증 기준

- 모든 화면에서 텍스트가 잘리지 않고 주요 CTA가 한눈에 보여야 한다.
- 모바일에서는 현재 과업과 다음 행동을 우선 노출한다.
- Figma와 브라우저의 화면 구조·간격·상태가 일치해야 한다.
- 코드 변경 없이 Figma만 수정하거나, Figma 확인 없이 코드를 임의 수정하지 않는다.
