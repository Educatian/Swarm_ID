You are improving the Korean redesign of **Design Tension Studio** (this repo, current branch `ko`). It is an undergrad-facing instructional-design tool. Design philosophy to PRESERVE:

- **Map-forward minimalism**: the D3 network map is the workspace. In 간단히 (simple) mode the map fills the screen and the left/right panels fold into a floating left **icon dock** (⊕ add node, 👁 perspectives+notes) as slide-in drawers. 자세히 (detailed) mode restores the full 3-panel layout.
- **Single indigo accent** (#4361ee), restrained button hierarchy (primary solid / ghost / quiet; sign-out is a quiet ghost, not a loud red).
- **Dark theme**, **WCAG-AA contrast** everywhere.
- **Korean, 해요체** (soft register). Four stakeholder lenses: 교사·학생·IT·행정.
- Do NOT reintroduce a procedural "checklist" framing; tensions are a network, not steps.

Preview harness (already built): `preview.html` is a static copy of `index.html` with `preview-demo.js` injected before `</body>`. `preview-demo.js` sets `window.__DTS_PREVIEW__` and defines `window.__dtsPreviewInject`; `boot()` in `app.js` calls it as its last step, injecting demo data and jumping straight into the student app (no login). If you edit `index.html`, REGENERATE `preview.html` by re-copying `index.html` and re-inserting `  <script src="./preview-demo.js"></script>` before `</body>`.

## Goal: continue the accessibility / responsive polish. Commit each coherent fix on `ko`.

1. **Narrow widths (400–820px)**: at ~400px the topbar action buttons (EN / identity / 튜토리얼 보기 / 로그아웃) stack vertically and push the map far down. Make the topbar compact and wrap cleanly so the map stays visible. Ensure the icon-dock + slide-in drawers fit at small widths, and there is **no horizontal overflow at any width** (the off-canvas drawers already use `overflow-x: clip` on `.density-simple .visualizer-layout` — keep that working).
2. **Hover / focus states**: verify color contrast on hover/focus for buttons, lens pills, nav items, and dock buttons. Add a visible keyboard focus ring (`:focus-visible`) where missing. Nothing should drop below WCAG AA on hover.
3. **Landing page (login)** and **instructor (admin role) views**: ensure they are fully Korean and contrast-clean on the dark theme (the contrast pass mostly covered the student map/home; double-check these two surfaces).
4. **One generated visual (exercise image generation)**: create a polished **1200×630 social-preview / OG image** for the landing page that matches the dark, indigo-accent, network-map aesthetic (abstract tension network, no lorem text). Save it under `frontend`-style public path or repo root (e.g. `./og-preview.png`) and wire it via `<meta property="og:image">` (+ `twitter:image`) in `index.html` (then regenerate `preview.html`).

## Guardrails
- Work ONLY on the `ko` branch, locally. **Do NOT `git push` and do NOT deploy.**
- Do NOT modify `supabase-config.js`, tokens, or any credentials.
- Keep changes surgical and consistent with the existing dark-theme override blocks at the end of `styles.css`.
- After each fix, commit with a clear message (English subject is fine). End commit messages with the existing Co-Authored-By trailer style if you add one, or omit it.
- When done, print a short summary of what changed and what (if anything) still needs human review.
